import { lazy } from 'react';

export const HomePage = lazy(() => import('./HomePage'));
export const CatalogPage = lazy(() => import('./CatalogPage'));
export const FavoritesPage = lazy(() => import('./FavoritePage'));
